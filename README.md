React example project.

Realised examples:

CRUD,
Authorization,
Lazy loading table,
Drag and Drop,
Some tests


Used: 
Redux(Ducks), 
Redux-Saga,
Immutable.js,
Reselect,
Redux-Form,
Firebase,
React-virtualized,
React-dnd, 
Tests: Jest + Enzyme